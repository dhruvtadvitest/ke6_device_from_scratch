version: 2.1
jobs:
  build:
   docker:
      - image: ubuntu:18.04
   steps:
      - run:
          name: "Dump"
          command: |
            apt-get update -y && apt-get upgrade -y && apt-get install wget curl git zip unzip gcc build-essential bc sudo python3 python3-pip -y
             git clone --single-branch https://github.com/DumprX/DumprX.git
             cd DumprX
             ./dumper.sh 'https://drive.google.com/file/d/1OI9XhtdVsdqxQG-hBziwYD3LsECgoVoc/view'
            
workflows:
  version: 2.1
  remote_builder:
    jobs:
      - build:
          context: org-global

